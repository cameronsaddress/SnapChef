# SnapChef Environment Variables
# ================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to version control

# API Configuration
SNAPCHEF_API_KEY=your-api-key-here
# Optional alias used by some backend deployments
APP_API_KEY=your-api-key-here

# Third-Party SDK Keys
TIKTOK_CLIENT_SECRET=your-tiktok-secret-here
FACEBOOK_APP_ID=your-facebook-app-id-here
FACEBOOK_CLIENT_TOKEN=your-facebook-token-here

# Optional: Development Settings
# API_BASE_URL=https://api-dev.snapchef.app
# ENABLE_DEBUG_LOGGING=true

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Fill in your actual API keys in the .env file
# 3. Recommended for Xcode builds: copy Config/LocalSecrets.xcconfig.example
#    to Config/LocalSecrets.xcconfig and set SNAPCHEF_API_KEY / APP_API_KEY there.
# 4. Optional override in Xcode: Edit Scheme → Run → Arguments → Environment Variables
# 5. For terminal builds, export the key before running xcodebuild:
#    export SNAPCHEF_API_KEY=your-api-key-here
#    xcodebuild -project ios/SnapChef.xcodeproj -scheme SnapChef ...
# 6. Never commit .env or Config/LocalSecrets.xcconfig to git
